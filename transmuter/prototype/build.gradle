final appMainClassName = 'transmuter.Main'
final appBaseName      = 'transmuter-prototype'
final appVersion       = transmuter.version

jar {
  baseName = appBaseName
  version =  appVersion
  manifest.mainAttributes('Main-Class' : appMainClassName)
}
