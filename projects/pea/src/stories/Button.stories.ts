import { Button } from '&/component'

const meta = {
  title: 'Example/Button',
  component: Button,
}

export const Primary = {
  args: {
    primary: true,
    label: 'Button',
  },
}

export default meta
