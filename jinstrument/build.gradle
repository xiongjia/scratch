subprojects {
  apply plugin: 'base'
  apply plugin: 'idea'
  apply plugin: 'java'

  buildDir = new File(rootProject.projectDir, "build/" + project.name)

  ext {
    jinstrument = ['version': '1.0']
  }
}

allprojects {
  repositories {
    mavenLocal()
    mavenCentral()
  }
}

