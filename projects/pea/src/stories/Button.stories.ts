import { Button } from '&/component'

const meta = {
  title: 'Example/Button',
  component: Button,
}

export default meta

export const Primary = {
  args: {
    primary: true,
    label: 'Button',
  },
}
