import { TestButton } from '&/component'

const meta = {
  title: 'Example/Button',
  component: TestButton,
}

export const Primary = {
  args: {
    primary: true,
    label: 'Button',
  },
}

export default meta
